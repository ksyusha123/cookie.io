const settings = {
    MAP_SIZE: 2000,
    START_RADIUS: 20,
    MESSAGES: {
        JOIN: 'join',
        INPUT: 'input',
        GAME_OVER: 'game_over',
        GAME_UPDATE: 'game_update',
    },
    CRITICAL_DISTANCE_BORDER: 0.8,
    CRITICAL_AREA_DIFF: 1.5,
    FOOD_RADIUS: 10,
    PLAYER_SPEED: 400,
};

module.exports = settings;