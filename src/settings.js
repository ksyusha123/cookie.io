const settings = {
    MAP_SIZE: 2000,
    START_RADIUS: 20,
    MESSAGES: {
        JOIN: 'join',
        INPUT: 'input',
        GAME_OVER: 'game_over',
        GAME_UPDATE: 'game_update',
    }
};

module.exports = settings;